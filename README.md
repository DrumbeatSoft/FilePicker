# FilePicker

文件选择
`
 FilePicker.from(MainActivity.this)
                        .chooseForMimeType()
                        .setMaxCount(3)
                        .setFileTypes("pdf")
                        .requestCode(1)
                        .start();
             
`
